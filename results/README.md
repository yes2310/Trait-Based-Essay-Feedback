# Leaf 결과 폴더

LEAF 전용 실행 결과를 저장하는 기본 경로입니다.

권장 하위 폴더:
- `trait_pretrain/`
- `holistic/`
- `trait_score/`

실행 스크립트는 작업 폴더 기준 상대 경로로 이 위치에 결과를 저장합니다.
따라서 `leaf_workspace` 전체를 다른 위치로 옮겨도 같은 구조로 계속 사용할 수 있습니다.
